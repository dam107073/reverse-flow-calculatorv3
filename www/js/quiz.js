(function () {
  "use strict";
  const STORAGE_KEY="reverse-flow-practice-quiz-session-v1";
  const content=document.getElementById("quizContent"),status=document.getElementById("quizStatus");
  const repository=new ReverseFlowResources.ResourceRepository("quiz");
  const el=(tag,className,text)=>{const node=document.createElement(tag);if(className)node.className=className;if(text!==undefined)node.textContent=text;return node;};
  let concepts=[],session=restore();
  function restore(){try{const value=JSON.parse(localStorage.getItem(STORAGE_KEY)||"null");return value&&Array.isArray(value.questions)&&value.questions.length&&value.completed===false?value:null;}catch(_error){return null;}}
  function save(){try{if(session&&!session.completed)localStorage.setItem(STORAGE_KEY,JSON.stringify(session));else localStorage.removeItem(STORAGE_KEY);}catch(_error){}}
  function button(label,onClick,className="reset-button"){const node=el("button",className,label);node.type="button";node.addEventListener("click",onClick);return node;}
  function selectField(label,values,current){const field=el("label","",label),select=el("select");values.forEach(value=>{const option=el("option","",value.label);option.value=value.id;if(value.id===current)option.selected=true;select.append(option);});field.append(select);return{field,select};}
  function showSetup(){session=null;save();const params=new URLSearchParams(location.search);const requested=params.get("category");const initial=ReverseFlowQuiz.CATEGORIES.some(item=>item.id===requested)?requested:"all";const setup=el("div","quiz-setup");const availability=el("p","helper",concepts.length?"Questions combine randomized calculations, operational applications, and saved website concepts.":"Concept updates are not cached on this device yet. Randomized calculation and application questions remain available offline.");availability.id="quizAvailability";setup.append(availability);const grid=el("div","quiz-setup-grid");const category=selectField("Category",ReverseFlowQuiz.CATEGORIES,initial);const difficulty=selectField("Difficulty",ReverseFlowQuiz.DIFFICULTIES.map(id=>({id,label:id[0].toUpperCase()+id.slice(1)})),"basic");const count=selectField("Questions",[5,10,20].map(id=>({id:String(id),label:String(id)})),"10");grid.append(category.field,difficulty.field,count.field);const error=el("p","learning-status");setup.append(grid,error,button("Start Quiz",()=>{try{session=ReverseFlowQuiz.createSession({concepts,category:category.select.value,difficulty:difficulty.select.value,count:Number(count.select.value),seed:Date.now()});save();renderQuestion();}catch(caught){error.textContent=caught.message;}}));content.replaceChildren(setup);}
  function renderQuestion(){if(!session)return showSetup();if(session.completed)return renderResults();const question=session.questions[session.currentIndex],answer=session.answers.find(item=>item.questionId===question.id);const shell=el("div","learning-shell");const progress=el("div","quiz-progress");progress.append(el("span","",`Question ${session.currentIndex+1} of ${session.questions.length}`),el("span","",`${session.answers.filter(item=>item.correct).length} correct`));shell.append(progress,el("p","helper",`${question.category.replaceAll("-"," ")} · ${question.difficulty}`),el("h3","",question.prompt));const choices=el("div","quiz-choice-list");question.choices.forEach((choice,index)=>{const choiceButton=button(choice,()=>{session=ReverseFlowQuiz.answerQuestion(session,index);save();renderQuestion();},"quiz-choice");choiceButton.setAttribute("aria-pressed",String(answer&&answer.choiceIndex===index));choiceButton.disabled=!!answer;if(answer&&index===question.correctIndex)choiceButton.classList.add("is-correct");if(answer&&index===answer.choiceIndex&&!answer.correct)choiceButton.classList.add("is-incorrect");choices.append(choiceButton);});shell.append(choices);if(answer){const feedback=el("div","quiz-feedback");feedback.setAttribute("role","status");feedback.tabIndex=-1;feedback.append(el("strong","",answer.correct?"Correct":"Not quite"),el("p","",`Correct answer: ${question.choices[question.correctIndex]}`),el("p","",question.explanation));if(question.math)feedback.append(el("p","formula-expression",question.math));shell.append(feedback,button(session.currentIndex===session.questions.length-1?"See Results":"Next Question",()=>{session=ReverseFlowQuiz.nextQuestion(session);save();renderQuestion();}));}shell.append(button("End Quiz",showSetup,"reset-button"));content.replaceChildren(shell);if(answer)shell.querySelector(".quiz-feedback").focus();}
  function renderResults(){save();const score=ReverseFlowQuiz.scoreSession(session),shell=el("div","learning-shell");const message=score.percentage>=80?"The main relationships are landing. Review any misses to keep them sharp.":score.percentage>=60?"You have a working base. Reviewing the missed concepts will strengthen the next round.":"Use the review below to identify which relationships to practice next.";shell.append(el("h3","","Quiz Complete"),el("div","quiz-result-score",`${score.percentage}%`),el("p","",`${score.correct} of ${score.total} correct.`),el("p","",message));const breakdown=el("div","quiz-review");score.byCategory.forEach(item=>breakdown.append(el("p","",`${item.category.replaceAll("-"," ")}: ${item.correct} of ${item.total}`)));shell.append(breakdown);const missed=score.missed.map(answer=>({answer,question:session.questions.find(q=>q.id===answer.questionId)}));if(missed.length){shell.append(el("h3","","Review Missed Questions"));const review=el("div","quiz-review");missed.forEach(({answer,question})=>{const card=el("article","quiz-review-item");card.append(el("strong","",question.prompt),el("p","",`Your answer: ${question.choices[answer.choiceIndex]}`),el("p","",`Correct answer: ${question.choices[question.correctIndex]}`),el("p","",question.explanation));review.append(card);});shell.append(review,button("Practice Missed Concepts",()=>{session=ReverseFlowQuiz.createRetrySession(session,concepts,Date.now());save();renderQuestion();}));}shell.append(button("Take Another Quiz",showSetup));content.replaceChildren(shell);}
  repository.subscribe(state=>{concepts=state.items||[];status.textContent=concepts.length?(state.message||(state.refreshing?"Checking for question updates…":"")):(["offline","error"].includes(state.status)?"Concept questions need an initial internet connection. Calculation practice remains available offline.":state.message);const availability=document.getElementById("quizAvailability");if(availability&&concepts.length)availability.textContent="Questions combine randomized calculations, operational applications, and saved website concepts.";if(!session&&content.childElementCount===0)showSetup();});
  if(session)renderQuestion();repository.refresh();
})();
